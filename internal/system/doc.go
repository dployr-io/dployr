// Package system implements the daemon's system service: sync, install/upgrade,
// registration, and health. Internal-only; external code should use pkg/core/system.
// Concrete system service implementations are in internal/system.
package system
