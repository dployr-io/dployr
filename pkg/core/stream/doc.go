// Package stream provides types and handlers for streaming application logs to clients.
// It defines core log streaming interfaces and API wiring for the daemon's web layer,
// with concrete log streaming implementations and helpers located in the internal/stream package.
package stream
